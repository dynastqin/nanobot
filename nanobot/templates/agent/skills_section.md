# Skills

The following skills extend your capabilities. To use a skill, read its SKILL.md by calling the `read_skill` tool with the skill name.
Unavailable skills need dependencies installed first — you can try installing them with apt/brew.

{{ skills_summary }}
